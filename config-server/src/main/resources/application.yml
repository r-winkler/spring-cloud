server:
  port: 8888

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: searchLocations: file:C:\dev\repos\spring-cloud\config-server
